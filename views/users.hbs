<!DOCTYPE html>
<html>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
<link rel="stylesheet" href="main.css">
<head>
    <meta charset="utf-8" />
    <title>IPG</title>
</head>

<body>
    <div class="container">
        <header class="d-flex justify-content-center py-3">
            <ul class="nav nav-pills">
                <li class="nav-item"><a href="#" class="nav-link">Создать пользователя</a></li>
                <input id="folderName" />
                <li class="nav-item"><a id="createFolder" href="#" class="nav-link">Создать каталог</a></li>
            </ul>
        </header>
        Всего пользователей {{length}}
        <div class="container-fluid pb-3">
            <div class="d-grid gap-3">
                <div class="bg-light border rounded-3">
                    <ul class="list-group">
                        {{#each users}}
                        <a class="list-group-item list-group-item-action {{#if this.disabled }}disabled{{/if}} " href="/users/{{this.name}}">
                            <span class="badge badge-success {{this.unit}}">{{this.unit}}</span>
                            <span>{{this.name}}</span> (<span>{{this.fullname}}</span>) - <span>{{this.lastLogin}}</span>
                        </a>
                        {{/each}}
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <script src="/users.js"></script>
</body>

</html>